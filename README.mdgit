node_modules/  .env  # ?? SecureFile - Client-side File Encryptionecho.echo A modern browser-based file encryption tool using AES-256.echo.echo ## ?? Previewecho (Add screenshot.png in repo root)echo.echo ## ?? Live Demoecho https://YOUR-USERNAME.github.io/secure-file-encryptionecho.echo ## ? Featuresecho - AES-GCM 256-bit encryptionecho - PBKDF2 key derivation (100k iterations)echo - Fully client-side (no uploads)echo - Drag & drop interfaceecho - Password strength indicatorecho.echo ## ?? Tech Stackecho - HTML + TailwindCSSecho - JavaScript (Web Crypto API)echo - jQueryecho.echo ## ?? Security Notesecho - Salt + IV included in encrypted fileecho - No data leaves your browserecho - Password is never storedecho.echo ## ?? Importantecho If you forget your password, your file cannot be recovered.echo.echo ## ?? Usageecho 1. Upload fileecho 2. Enter passwordecho 3. Encrypt / Decrypt)  initgit add .git commit -m "Initial commit: SecureFile encryption tool"gh repo create secure-file-encryption --public --source=. --remote=origin --push
